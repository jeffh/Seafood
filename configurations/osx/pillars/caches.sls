caches:
    # where the workstation stores packages
    packages: '/tmp/salt/packages/'
    # where the workstation stores scripts
    scripts: '/tmp/salt/script/'
    # where application hashes reside so salt know when to update apps
    hashes: '/tmp/salt/hashes/'
    # where pivotal_workstation_base scripts work in
    tmp: '/tmp/salt/tmp/'
